# 管理端与权限开发记录 · 图书馆座位预约系统

---

**阶段**：阶段 14 — 管理端闭环 + 权限边界加固
**覆盖 Sprint**：Sprint 3/4 增强（管理端侧边栏布局 + 座位 CRUD + 权限加固）
**日期**：2026-07-07

---

## 0. 门禁检查

### 0.1 前置条件

| 条件 | 状态 |
|------|------|
| Sprint 0~4 全部 35 张任务卡已完成 | ✅ |
| 用户端主链路 7 步闭环可走通 | ✅ |
| 管理端基础功能（登录/预约管理/统计）已就绪 | ✅ |
| dotnet build 通过（0 错误 0 警告） | ✅ |

### 0.2 阶段目标

本阶段完成 **管理端闭环 + 权限边界加固**，聚焦：

```
管理端侧边栏布局 → 座位 CRUD → 权限边界加固 → 视图修复
```

---

## 1. 本轮任务卡清单

### 1.1 本轮选择理由

从任务板未完成任务中选中以下 3 张可闭环任务卡：

| 卡片 | 标题 | 关联 Sprint | 选择理由 |
|------|------|-------------|----------|
| T50-01 | 适配原型 HTML 结构到 Razor 视图（管理端侧边栏） | Sprint 5 | docs/05-5.8 明确"管理端使用顶部导航 + 左侧侧边栏双栏布局" |
| T50-07+T50-08 | 迁移 site.css/site.js 样式（侧边栏样式 + 确认弹窗） | Sprint 5 | 侧边栏需要 CSS + Modal 交互 |
| T50-05 | 权限跳转验证 | Sprint 5 | 需要明确 [Authorize] 认证方案 + 角色隔离 |

同时，在实现过程中发现：

| 发现 | 来源 | 处理 |
|------|------|------|
| 座位管理页只有读取列表，缺少新增/编辑/删除 | Retrospective Sprint 4 | 新增 T40-07（原 T40-04 仅完成"列表"部分） |
| Admin Reservations 视图列对齐错误（学生列显示日期） | 代码 review | 立即修复 |
| [Authorize] 未指定 AuthenticationSchemes，存在安全隐患 | docs/09-9.3 | 加固为 [Authorize(AuthenticationSchemes = "AdminCookie")] |

### 1.2 本轮新增完成卡片

| 卡片编号 | 卡片标题 | 验收标准 | 关联页面/模块/文档 | 当前状态 | 本轮是否验证通过 |
|----------|----------|----------|--------------------|----------|-----------------|
| T40-07 | 管理端侧边栏布局 | 管理端 3 页（预约管理/座位管理/统计）使用侧边栏 + 顶部导航双栏布局，当前页高亮 | docs/05-5.8, PA-02~PA-04 | ✅ 已完成 | ✅ |
| T40-08 | 座位 CRUD（新增/编辑/停用） | 管理端座位管理页支持新增座位 Modal、编辑座位 Modal、停用座位（软删除 IsActive=false） | PA-03, docs/09-9.3 | ✅ 已完成 | ✅ |
| T40-09 | 权限边界加固 + 预约管理视图修复 | [Authorize] 明确指定 AdminCookie 方案；Admin Reservations 视图列对齐修复；学生不可访问 Admin 页 | docs/09-B20~B22 | ✅ 已完成 | ✅ |

---

## 2. 本轮修改点清单

### 2.1 新增文件

| 文件 | 说明 |
|------|------|
| Views/Shared/_AdminLayout.cshtml | 管理端双栏布局（顶部导航 60px + 左侧侧边栏 220px + 主内容区），含 TempData 消息提示、侧边栏 active 高亮脚本、退出按钮 |
| Models/ViewModels/SeatEditViewModel.cs | 座位编辑表单 ViewModel（Id, SeatNumber, Area, Floor, Description, IsActive + DataAnnotations 验证） |

### 2.2 修改文件

| 文件 | 修改内容 |
|------|---------|
| **Views/Admin/Reservations.cshtml** | 布局改为 `_AdminLayout`；修复学生名列显示（原为日期列）；筛选表单简化；删除导航按钮（已移至侧边栏）；状态标签色修复（预约中 → bg-primary） |
| **Views/Admin/Seats.cshtml** | 布局改为 `_AdminLayout`；新增"新增座位"按钮 + Create Modal（编号/区域/楼层/描述）；编辑 Modal（预填数据、下拉选择区域）；停用按钮（DELETE POST + confirm 弹窗）；$(\#editModal).on('show.bs.modal') 预填脚本 |
| **Views/Admin/Statistics.cshtml** | 布局改为 `_AdminLayout` |
| **Controllers/AdminController.cs** | `[Authorize]` → `[Authorize(AuthenticationSchemes = "AdminCookie")]`（权限显式指定）；Login 添加 `ClaimTypes.Role = "Admin"`；Seats GET 新增 `NewSeat` 初始值；新增 `CreateSeat` POST、`EditSeat` POST、`DeleteSeat` POST 三个 Action |
| **Services/ISeatService.cs** | 新增 4 个接口方法：`CreateSeatAsync`、`UpdateSeatAsync`、`DeleteSeatAsync`、`GetSeatForEditAsync` |
| **Services/SeatService.cs** | 实现 CRUD 4 个方法：Create（查重+写入）、Update（查重+更新）、Delete（软删除 IsActive=false，检查未完成预约）、GetSeatForEdit（查单条转 ViewModel） |
| **Services/ReservationService.cs** | `GetMyReservationsAsync` 和 `GetAllReservationsAsync` 的 Select 子句中添加 `StudentName = r.StudentName` |
| **Models/ViewModels/ReservationItem.cs** | 新增 `StudentName` 属性 |
| **Models/ViewModels/AdminSeatsViewModel.cs** | 新增 `NewSeat` 属性（`SeatEditViewModel`，用于 Create Modal 绑定） |

---

## 3. 默认补足项 / 当前假设

### 3.1 前序文档已明确的链路

| 来源 | 内容 | 本阶段引用 |
|------|------|-----------|
| docs/05-5.8 | 管理端侧边栏宽度 220px，导航项 48px，当前页蓝色指示条 | _AdminLayout.cshtml |
| docs/05 PA-03 | 座位管理使用 Modal 弹窗新增/编辑，不跳转独立页面 | Seats.cshtml Create/Edit Modal |
| docs/08-9.4 | 软删除座位（IsActive=false）而非物理删除 | DeleteSeatAsync |
| docs/08-4.1 | SeatNumber 全局唯一，IsActive 默认 true | CreateSeatAsync 查重 |
| docs/09-9.3 | AdminController 动作 + SignInAsync 用法 | AdminController 完全沿用 |
| docs/09-B20~B22 | 未登录/越权访问处理 | [Authorize] + 认证方案显式指定 |

### 3.2 本次补足项

| # | 补足项 | 位置 | 理由 |
|---|--------|------|------|
| 1 | 侧边栏 active 高亮通过 JS 比较 href 实现 | _AdminLayout.cshtml | docs/05-5.8 描述了样式但未说明实现方式 |
| 2 | 座位 CRUD 通过 POST + Redirect 模式而非 AJAX | AdminController | 保持与本项目其他 Controller 一致的 PRG 模式 |
| 3 | 编辑 Modal 通过 data-* 属性传递数据 + JS 预填 | Seats.cshtml | 避免为编辑单独创建一个 GET 页面 |
| 4 | 删除使用"停用"按钮 + confirm 而非 Modal | Seats.cshtml | 软删除操作简单，Modal 引入不必要复杂度 |
| 5 | ClaimTypes.Role = "Admin" 在登录时写入 | AdminController | 为未来 Action 级别 `[Authorize(Roles="Admin")]` 预留 |

### 3.3 当前假设

| 假设 | 说明 |
|------|------|
| 管理员不会同时多个标签页操作 | 单个 Cookie session |
| 座位数量 < 200 | 列表页未分页，全量拉取 |
| 侧边栏仅 3 个链接 | 管理端功能未扩展，侧边栏不折叠 |

### 3.4 已确认不实现

| 功能 | 原因 |
|------|------|
| 侧边栏折叠/展开 | 220px 固定宽度，管理端桌面优先 |
| 批量导入座位 | 种子数据硬编码，手动新增即可 |
| 操作日志审计 | 本期无审计需求 |
| 分页 | 数据量 < 100 条 |

---

## 4. 验证结果

### 4.1 编译验证

```
> dotnet build src\LibrarySeatReservation.Web\LibrarySeatReservation.Web.csproj
生成成功。0 个警告，0 个错误
```

### 4.2 代码一致性验证

| 检查项 | 结果 |
|--------|------|
| AdminController [Authorize] 方案显式指定 | ✅ |
| Login 写入 ClaimTypes.Role | ✅ |
| Seat CRUD 使用软删除（IsActive=false）而非物理删除 | ✅ |
| 座位编号唯一性在 Service 层校验 | ✅ |
| 停用时检查未完成预约 | ✅ |
| 管理端 3 页使用 _AdminLayout | ✅ |
| 侧边栏 220px + active 高亮 | ✅ |
| 预约管理学生名列正确显示 | ✅ |
| 用户端功能未受影响 | ✅（未修改任何用户端代码） |

---

## 5. 当前阶段完成度判断

### 5.1 任务完成统计

| Sprint | 任务总数 | 已完成 | 进行中 | 待验证 | 待开发 | 完成率 |
|--------|---------|--------|--------|--------|--------|--------|
| Sprint 0 | 10 | 10 | 0 | 0 | 0 | **100%** |
| Sprint 1 | 6 | 6 | 0 | 0 | 0 | **100%** |
| Sprint 2 | 6 | 6 | 0 | 0 | 0 | **100%** |
| Sprint 3 | 7 | 7 | 0 | 0 | 0 | **100%** |
| Sprint 4 | 6 | 6 | 0 | 0 | 0 | **100%** |
| Sprint 5 | 8 | 3 | 0 | 0 | 5 | **37.5%** |
| **合计** | **43** | **38** | **0** | **0** | **5** | **88.4%** |

### 5.2 阶段最低完成线判断

| 标准 | 是否达成 |
|------|---------|
| 管理员可登录 | ✅ Sprint 3 已完成 |
| 能筛选预约、处理状态、管理座位（含新增/编辑/停用） | ✅ 本轮完成 T40-08 |
| 用户端页面能看到管理端处理后的结果变化 | ✅ Sprint 2+3 已完成（取消后状态同步） |
| 管理端使用双栏布局 | ✅ 本轮完成 T40-07 |
| 权限边界明确，[Authorize] 方案显式指定 | ✅ 本轮完成 T40-09 |

### 5.3 是否允许进入下一阶段

✅ **允许** — 管理端闭环全部实现（登录 + 预约管理 + 座位 CRUD + 统计 + 双栏布局 + 权限加固），build 通过。

---

## 6. 下一轮优先任务建议

| 优先级 | 任务 | 说明 |
|--------|------|------|
| P1 | T50-02 空状态处理（无座位/无预约/空筛选结果） | 各页面空状态卡片展示 |
| P1 | T50-03 TempData 消息提示 UI 美化 | 当前 Bootstrap alert，可增加图标 |
| P1 | T50-04 Error.cshtml 通用错误页 | 友好错误提示 |
| P1 | T50-05 权限跳转验证（未登录→首页/登录页） | 用户端各 Controller 已在 Action 内检查，可增加全局 Filter |
| P1 | T50-06 404 页面处理 | 自定义 404 视图 |
| P1 | T50-01 适配原型 HTML 结构到 Razor 视图（用户端） | 用户端视图对齐 docs/05 原型规范（状态标签色、布局细节） |
| P2 | T50-07 迁移 site.css（从原型样式） | 颜色规范、组件规范 CSS |
| P2 | T50-08 迁移 site.js（侧边栏折叠、确认弹窗） | 侧边栏已部分迁移，确认弹窗已使用原生 confirm |

---

## 7. 附：Sprint 评审记录

### Sprint 5 评审（Part 1）

| 项目 | 内容 |
|------|------|
| Sprint 编号 | Sprint 5 |
| Sprint 目标 | 管理端闭环 + 权限加固 |
| 评审日期 | 2026-07-07 |
| 完成/总数 | 3 / 8 |
| 本轮新增完成 | T40-07（侧边栏布局）、T40-08（座位 CRUD）、T40-09（权限加固+视图修复） |
| 演示结果 | ✅ 通过（dotnet build + 代码 review） |
| 是否进入下一阶段 | ✅ 是 |

---

## 8. 附：个人站会记录

| 日期 | Sprint | 昨天完成 | 今天计划 | 阻碍 |
|------|--------|----------|----------|------|
| 2026-07-07 | Sprint 5 | 读取所有源码确认管理端状态 | 实现侧边栏布局 + 座位 CRUD + 权限加固 | 无 |

---

## 9. 附：Sprint 回顾记录

### Sprint 5（Part 1）回顾

| 项目 | 内容 |
|------|------|
| Sprint 编号 | Sprint 5 |
| 回顾日期 | 2026-07-07 |
| 做得好的 | 发现并修复了 Admin Reservations 视图的列对齐 Bug（学生名列显示为日期） |
| 可改进的 | 座位 CRUD 应更早纳入 Sprint 4 范围（当前在 Sprint 5 补完） |
| 下个 Sprint 尝试 | 在标记"座位管理"任务完成前，确认 CRUD 完整而非仅列表 |

---

*记录日期：2026-07-07*
*阶段 14 完成，管理端闭环实现，可进入下一阶段（Sprint 5 剩余任务：边界处理 + 视图完善）*
