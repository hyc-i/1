# 阶段 16 联调测试与缺陷闭环 · 审计报告

---

**审计日期**：2026-07-14
**审计范围**：`README.md`、`tests/`（package.json、playwright.config.js、specs/、smoke-test.mjs）、`docs/16-联调测试与缺陷闭环.md`、`Docs/项目任务板与迭代记录.md`、`src/` 全部源文件
**审计基线**：docs/09（关键链路详细设计）、docs/13/14/15 审计报告、阶段 15 审计报告中的跨阶段遗留问题

---

## A. 总体判断

**结论：阶段 16 的联调测试主体工作质量较高——Playwright 自动点击 12/12 通过、脚本烟雾 9/9 通过、测试场景覆盖了用户端 7 步 + 管理端 5 步主链路和 5 条关键规则，Bug 闭环清晰，回归/兼容性/默认补足测试项均有显式记录。但本轮审计发现 2 个 P0 文档/数据失真问题（任务板 Sprint 5 完成状态与实际不符、docs/16 修改文件清单包含不存在的根目录 package.json），以及跨阶段遗留的 2 个 P0 代码问题（唯一索引 `.IsUnique()` 第 4 轮未修复、Edit Modal 楼层字段第 3 轮未修复）仍未处理。阶段最低完成线（主流程可演示 + 关键异常已处理 + 双端数据一致 + 测试/Bug 闭环记录）在测试执行层面已达成，但文档真实性和跨阶段代码缺陷构成阻塞。**

具体而言：

- ✅ **测试环境判定合理**：Level B（安装 + 补齐 + 执行），Node.js v26.3.1 + Edge v150.0.4078.65 就绪，Playwright `channel: 'msedge'` 配置正确，未执行 `npx playwright install` 下载浏览器。
- ✅ **Playwright 自动点击完整执行**：12 个测试用例（用户端 6 + 管理端 6），覆盖首页加载/登录/座位列表/预约提交/我的预约/取消预约 + 管理端登录/预约管理/座位管理/统计/登出/错误密码。`test-results/.last-run.json` 确认 `{"status":"passed","failedTests":[]}`。
- ✅ **脚本烟雾测试完整执行**：9 个 HTTP 端点检查（首页/标题关键字/座位列表302/管理端登录200/管理端4个端点302/静态资源200/我的预约302），Node.js `.mjs` 脚本，编码兼容。
- ✅ **Bug 闭环清晰**：3 个 Bug 均为测试用例适配问题（登出 URL 断言、取消后重复匹配、预约冲突），非代码 Bug，均已修复关闭。
- ✅ **回归测试已执行**：第 1 轮 10/12 失败（2 个测试断言问题），修复后第 2 轮 12/12 + 9/9 全通过。
- ✅ **兼容性测试已记录**：msedge 通过，Chrome 推断兼容（Chromium 同源），布局响应式验证通过。
- ✅ **默认补足测试项显式声明**：未登录端点 302 验证、静态资源可访问性、PowerShell→Node.js 脚本迁移。
- ✅ **联调链路验证**：学生端预约→管理端可见、学生端取消→管理端可见、管理端取消→学生端可见，均通过自动点击验证。
- ✅ **README 已增量更新**：新增"自动化测试"章节（Playwright + 脚本烟雾命令 + 结果表），当前阶段更新为"联调测试与缺陷闭环已完成"，待完成功能保留并标注 Sprint。
- ⚠️ **docs/16 内部一致性小问题**：第 4.2 节声称"用户端 7 个测试用例"但实际 user-flow.spec.js 只有 6 个 test 块；第 16.2 节声称修改了"package.json（项目根目录）"但根目录不存在 package.json，实际只有 `tests/package.json`。
- ❌ **任务板 Sprint 5 完成状态失真**：docs/16 第 11.1 节声称 Sprint 5 "8 / 8 100%"，但任务板"待开发"区域仍列出 T50-01 ~ T50-08 共 8 张 Sprint 5 卡片，Sprint 5 分区仍显示"3 / 8 37.8%"。两份数据矛盾。
- ❌ **docs/15 开发记录仍不存在**：`Docs/15-功能完善与体验优化记录.md` 仍然缺失，仅有审计文件（由审计人生成）。Sprint 5 的 8 张任务卡没有任何开发记录支撑。
- ❌ **P0-01 唯一索引跨阶段第 4 轮未修复**：`AppDbContext.cs:45-46` 仍为 `HasDatabaseName("IX_Reservation_Seat_Time")`，缺少 `.IsUnique()`。任务板一致性修订跟踪仍为"⬜ 未处理"。
- ❌ **P0-02 Edit Modal 楼层字段跨阶段第 3 轮未修复**：`Admin/Seats.cshtml:34-39` 编辑按钮仍缺少 `data-floor` 属性；`SeatItem.cs` 仍缺少 `Floor` 属性。
- ❌ **9+ 项 P1 代码问题跨阶段未修复**：日期筛选（5 处）、时区（2 处）、导航登录态、统计查询策略。

**阶段最低完成线判定**：阶段 16 本身的测试执行和 Bug 闭环已达标（主流程可演示 + 关键异常已处理 + 双端数据一致 + 回归/兼容性/Bug 记录完整），但任务板 Sprint 5 状态失真 + docs/15 开发记录缺失 + 跨阶段 P0 代码缺陷未修复构成文档真实性和代码质量阻塞。**本轮审计建议：修正任务板 Sprint 5 状态 + 补齐 docs/15 开发记录 + 修复 P0-01/P0-02 后可宣布阶段 16 完成。**

---

## B. 发现的问题清单

### B-1 · 阻塞级问题（必须修复）

| 编号 | 问题 | 严重度 | 说明 |
|------|------|--------|------|
| **P0-1** | 任务板 Sprint 5 完成状态与 docs/16 数据矛盾 | **P0** | docs/16 第 11.1 节声称 Sprint 5 "8 / 8 100%"，但任务板"待开发"仍列出 T50-01~T50-08（8 张卡片），Sprint 5 分区仍显示"3 / 8 37.8%"。docs/16 第 0.1 节门禁声称"Sprint 0~4 全部 43 张任务卡已完成"，但 Sprint 0~4 实际为 38 张（10+6+6+7+9=38），加上 Sprint 5 的 T40-07/T40-08/T40-09 为 41 张，仍非 43 张 |
| **P0-2** | docs/16 修改文件清单包含不存在的根目录 package.json | **P0** | 第 16.2 节声称"package.json（项目根目录）新增 test:smoke 和 test:playwright 脚本"，但根目录不存在 package.json，实际只有 `tests/package.json` |
| **P0-3** | `IX_Reservation_Seat_Time` 缺少 `.IsUnique()`，跨阶段第 4 轮未修复 | **P0** | `AppDbContext.cs:45-46`，任务板一致性修订跟踪 P0-01 仍为"⬜ 未处理"。阶段 13/14/15/16 四轮审计均标记 |
| **P0-4** | Admin/Seats 编辑 Modal 楼层字段未传递，跨阶段第 3 轮未修复 | **P0** | `Admin/Seats.cshtml:34-39` 编辑按钮缺少 `data-floor`；`SeatItem.cs` 缺少 `Floor` 属性。阶段 14/15/16 三轮审计均标记 |

### B-2 · 文档层面问题

| 编号 | 问题 | 严重度 | 说明 |
|------|------|--------|------|
| **D-1** | docs/15 开发记录（`15-功能完善与体验优化记录.md`）仍不存在 | P1 | 仅有审计文件（审计人生成），无执行过程记录。Sprint 5 的 8 张任务卡无开发记录支撑 |
| **D-2** | docs/16 第 4.2 节声称"用户端 7 个测试用例"但实际为 6 个 | P2 | user-flow.spec.js 包含 6 个 test 块：L1-1+L1-2、L1-1、L1-3、L1-4、L1-5+L1-6、L1-7。合计应为用户端 6 + 管理端 6 = 12 |
| **D-3** | docs/16 第 11.1 节 Sprint 合计数据有误 | P2 | 声称总任务 51 张、已完成 49 张，但 Sprint 0~6 实际为 10+6+6+7+9+8+5=51 张，已完成应为 10+6+6+7+9+3+3=44 张（Sprint 5 仅 3 张、Sprint 6 仅 3 张） |

### B-3 · 代码与设计文档的偏差（跨阶段遗留，本轮未修复）

| 编号 | 问题 | 严重度 | 说明 |
|------|------|--------|------|
| **H-P1-1** | `SeatService.GetHomeStatsAsync` 未按日期筛选已占用座位 | P1 | `SeatService.cs:22-23` 缺少 `r.ReservationDate == now.Date` |
| **H-P1-2** | `SeatService.GetAllSeatsAsync` 缺少日期筛选 | P1 | `SeatService.cs:38-39` 同上 |
| **H-P1-3** | `SeatService.GetAllSeatsForAdminAsync` 缺少日期筛选 | P1 | `SeatService.cs:89-90` 同上 |
| **H-P1-4** | `SeatService.GetSeatDetailAsync` 缺少日期筛选 | P1 | `SeatService.cs:60-61` 同上 |
| **H-P1-5** | `AdminService.GetStatisticsAsync` 热门区域未限定当天 | P1 | `AdminService.cs:42-43` 统计历史全部预约 |
| **H-P1-6** | `ReservationsController.Create GET` 默认日期使用 UTC | P1 | `ReservationsController.cs:40` 使用 `DateTime.UtcNow.Date.AddDays(1)` |
| **H-P1-7** | `ReservationService.CreateReservationAsync` 日期校验使用 UTC | P1 | `ReservationService.cs:28` 使用 `DateTime.UtcNow.Date` |
| **H-P1-10** | `_Layout.cshtml` 导航未区分已登录/未登录态 | P1 | 用户端导航栏无登录状态感知 |
| **H-P1-12** | `AdminService.GetStatisticsAsync` 未按 D08-8.3 一次查询策略 | P1 | 4 次独立查询 |

### B-4 · 测试质量问题（非阻塞，建议改进）

| 编号 | 问题 | 说明 |
|------|------|------|
| T-1 | `playwright.config.js` webServer.cwd 使用硬编码绝对路径 | `D:\\AIWeb\\second-classroom-manager` 不可移植，其他环境 clone 后会失败 |
| T-2 | 用户端测试用例存在条件跳过 | L1-4、L1-5+L1-6、L1-7 中 `if (await btn.count() > 0)` 使得找不到元素时静默通过，降低测试有效性 |
| T-3 | 脚本烟雾测试仅覆盖 GET + 未登录端点 | 未验证已登录状态下的页面内容正确性 |
| T-4 | smoke-test.ps1 存在但未被文档引用 | docs/16 仅提及 `.mjs` 版本，PowerShell 版本为遗留文件 |

### B-5 · 非阻塞历史问题（记录为交付前剩余修订项）

| 编号 | 问题 | 说明 |
|------|------|------|
| H-1 | 座位状态标签颜色未按 D05 规范 | D05-1.2 规定浅底深字 pill 样式，实际使用 Bootstrap 默认 badge |
| H-2 | `StudentContext.Logout()` 使用 `Session.Clear()` | 应移除特定键值 |
| H-3 | 项目存在两个独立代码库 | 根目录旧项目与 src 新项目共存 |

---

## C. 必改项

### C-1 · 任务板 Sprint 5 状态修正（P0-1）

**位置**：`Docs/项目任务板与迭代记录.md`

**问题**：docs/16 声称 Sprint 5 "8 / 8 100%"，但任务板数据矛盾。

**应修正为**：
- 若 Sprint 5 的 T50-01~T50-08 确实未开发：任务板 Sprint 5 分区保持"3 / 8"，docs/16 第 11.1 节 Sprint 5 行改为"8 | 3 | 0 | 0 | 5 | 37.5%"
- 若 Sprint 5 的 T50-01~T50-08 已在某轮开发：需补充 docs/15 开发记录并更新任务板"待开发"→"已完成"
- docs/16 第 0.1 节门禁"Sprint 0~4 全部 43 张任务卡已完成"中的"43"应核实为正确数字

### C-2 · docs/16 修改文件清单修正（P0-2）

**位置**：`docs/16-联调测试与缺陷闭环.md` 第 16.2 节

**当前**：`package.json（项目根目录）新增 test:smoke 和 test:playwright 脚本`

**应改为**：`tests/package.json 新增 test:smoke 和 test:playwright 脚本`（或删除此条，因为 tests/package.json 已在 16.1 节"新增文件"中列出）

### C-3 · P0-01 唯一索引修复（跨阶段第 4 轮）

**位置**：`src/LibrarySeatReservation.Web/DataAccess/AppDbContext.cs:45-46`

**当前代码**：
```csharp
e.HasIndex(r => new { r.SeatId, r.ReservationDate, r.StartTime, r.EndTime })
    .HasDatabaseName("IX_Reservation_Seat_Time");
```

**应改为**：
```csharp
e.HasIndex(r => new { r.SeatId, r.ReservationDate, r.StartTime, r.EndTime })
    .IsUnique()
    .HasDatabaseName("IX_Reservation_Seat_Time");
```

**同步修复**：重新生成 EF Core 迁移 + 更新任务板 P0-01 一致性修订跟踪状态

### C-4 · Edit Modal 楼层字段修复（跨阶段第 3 轮）

需同步修复 3 个位置：

1. `src/LibrarySeatReservation.Web/Models/ViewModels/SeatItem.cs` — 添加 `public string? Floor { get; set; }`
2. `src/LibrarySeatReservation.Web/Views/Admin/Seats.cshtml:34-39` — 编辑按钮补充 `data-floor="@seat.Floor"`
3. `src/LibrarySeatReservation.Web/Services/SeatService.cs` — `GetAllSeatsForAdminAsync` 的 Select 中赋值 `Floor = s.Floor`

### C-5 · docs/16 内部数据修正

- 第 4.2 节：用户端测试用例数从"7 个"修正为"6 个"（或合并计数方式说明）
- 第 11.1 节：Sprint 5 完成数修正为"3 / 8"（或补充 Sprint 5 开发记录后保持"8 / 8"）
- 第 11.1 节：合计已完成数从"49"修正为实际值（取决于 Sprint 5 状态）

---

## D. 建议优化项

| 编号 | 建议 | 优先级 |
|------|------|--------|
| O-1 | `playwright.config.js` webServer.cwd 改为相对路径或动态获取（`__dirname`） | 中 |
| O-2 | 用户端测试用例移除条件跳过，改为显式断言元素存在 | 中 |
| O-3 | 脚本烟雾测试补充已登录状态下的页面内容检查 | 低 |
| O-4 | 清理 `tests/smoke-test.ps1`（已被 `.mjs` 替代）或在文档中说明其用途 | 低 |
| O-5 | 修复阶段 13/14/15 遗留的 P1-1~P1-7（日期筛选 + 时区） | 中 |
| O-6 | `_Layout.cshtml` 用户端导航增加登录态判断 | 中 |
| O-7 | 座位状态标签改用 D05 定义的浅底深字 pill 样式 | 低 |

---

## E. 是否可以进入下一步"交付、仓库提交与复盘"

**有条件可以进入，但需先完成以下前置条件：**

### 前置条件清单

| 条件 | 当前状态 | 必要性 |
|------|----------|--------|
| Playwright 自动点击 12/12 通过 | ✅ 已满足 | 必须 |
| 脚本烟雾测试 9/9 通过 | ✅ 已满足 | 必须 |
| 主流程可完整演示（用户端 7 步 + 管理端 5 步） | ✅ 已满足 | 必须 |
| 关键规则已验证（重复冲突/归属校验/停用不可预约） | ✅ 已满足 | 必须 |
| 联调双端数据一致 | ✅ 已满足 | 必须 |
| Bug 闭环（3/3 已关闭） | ✅ 已满足 | 必须 |
| 回归测试通过 | ✅ 已满足 | 必须 |
| 兼容性测试记录 | ✅ 已满足 | 必须 |
| 任务板 Sprint 5 状态与实际一致 | ❌ 不一致 | 必须 |
| docs/16 修改文件清单准确 | ❌ 包含不存在文件 | 必须 |
| docs/15 开发记录存在 | ❌ 不存在 | 必须 |
| P0-01 唯一索引代码修复 | ❌ 第 4 轮未修复 | 必须 |
| P0-02 Edit Modal 楼层 Bug 修复 | ❌ 第 3 轮未修复 | 必须 |

### 建议进入路径

```
第一步（必须）：修正任务板 Sprint 5 完成状态 + docs/16 内部数据（P0-1, P0-2, C-5）
第二步（必须）：补齐 docs/15 开发记录（或确认 Sprint 5 未执行并更新任务板）
第三步（必须）：修复 P0-01（.IsUnique()）+ 生成迁移
第四步（必须）：修复 P0-02（Edit Modal 楼层 Bug）
第五步（建议）：修复 P1-1~P1-7 日期筛选和时区问题
第六步：推送远端 + 宣布阶段 16 完成，进入最终交付
```

**判断依据**：阶段 16 的测试执行工作本身质量较高，Playwright 自动点击和脚本烟雾测试均已通过，Bug 闭环清晰。但任务板数据失真（Sprint 5 完成状态矛盾）和 docs/16 修改文件清单错误构成文档层面 P0 阻塞，加上跨阶段遗留的 2 个 P0 代码缺陷（唯一索引 + Edit Modal 楼层）第 3~4 轮仍未修复，不满足进入最终交付的条件。修复上述 4 个 P0 问题后可宣布阶段 16 完成。

---

*审计人：opencode*
*审计日期：2026-07-14*
