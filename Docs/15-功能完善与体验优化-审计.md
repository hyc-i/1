# 阶段 15 功能完善与体验优化 · 审计报告

---

**审计日期**：2026-07-13
**审计范围**：`Docs/15-功能完善与体验优化记录.md`、`Docs/项目任务板与迭代记录.md`、`README.md`、`src/` 全部源文件
**审计基线**：docs/05（页面卡与 UI 规范）、docs/08（数据库设计）、docs/09（关键链路详细设计）、阶段 14 审计报告

---

## A. 总体判断

**结论：阶段 15 无任何实质交付。开发记录文档缺失，任务板 Sprint 5/6 所有任务卡仍停留在"待开发"，源代码与阶段 14 审计时完全一致，未观察到任何新增或修改。本轮审计判定为"阶段 15 未开始执行"，需从头完成 Sprint 5 全部任务卡。**

具体而言：

- ❌ **开发记录缺失**：`Docs/15-功能完善与体验优化记录.md` 不存在，无任何阶段 15 的执行痕迹。
- ❌ **任务板未更新**：`Docs/项目任务板与迭代记录.md` Sprint 5（T50-01 ~ T50-08）和 Sprint 6（T60-01 ~ T60-04）全部 12 张任务卡仍停留在"⬜ 待开发"，无任何卡片移至"✅ 已完成"。
- ❌ **README 未更新**：`README.md` 仍停留在阶段 14 状态（"进入边界与错误处理阶段（Sprint 5）"），未更新为阶段 15 内容。
- ❌ **源代码无变更**：逐一比对阶段 14 审计时的所有关键文件，均未发现新增或修改：
  - `Error.cshtml`：仅 6 行默认模板，未新增 404 页面或自定义错误页。
  - `site.css`：仅 31 行默认模板，无业务样式。
  - `site.js`：仅 4 行注释，无业务脚本。
  - `_Layout.cshtml`：无登录态判断，无欢迎语/退出按钮。
  - `_AdminLayout.cshtml`：与阶段 14 一致，无变更。
  - 所有 Service/Controller/Model 文件：无变更。
- ❌ **阶段 14 遗留 P0 仍未修复**：
  - P0-01：`AppDbContext.cs:45-46` 的 `IX_Reservation_Seat_Time` 仍缺少 `.IsUnique()`。
  - P0-02：`Admin/Seats.cshtml:34-39` 编辑按钮仍缺少 `data-floor` 属性；`SeatItem.cs` 仍缺少 `Floor` 属性。
- ❌ **阶段 13/14 遗留 P1 仍未修复**：日期筛选（5 处）、时区（2 处）、导航登录态、统计查询策略等共 9+ 项 P1 问题无任何处理。

**阶段最低完成线判定**：阶段 15 无任何交付物（无开发记录、无任务卡完成、无代码变更），**不满足最低完成线**。建议从头执行 Sprint 5，至少完成 T50-01（Error 页面）+ T50-02（404 页面）+ T50-03（空状态优化）三张核心任务卡后重新审计。

---

## B. 发现的问题清单

### B-1 · 阻塞级问题（必须修复）

| 编号 | 问题 | 严重度 | 说明 |
|------|------|--------|------|
| **P0-1** | 阶段 15 完全无交付 | **P0** | 无开发记录、无任务卡完成、无代码变更，整个阶段未执行 |
| **P0-2** | `IX_Reservation_Seat_Time` 缺少 `.IsUnique()`，跨阶段遗留第 3 轮未修复 | **P0** | `AppDbContext.cs:45-46`，阶段 13/14/15 三轮审计均标记为 P0，始终未修复 |
| **P0-3** | Admin/Seats 编辑 Modal 楼层字段未传递，跨阶段遗留第 2 轮未修复 | **P0** | `Admin/Seats.cshtml:34-39` 缺少 `data-floor`，`SeatItem.cs` 缺少 `Floor` 属性，阶段 14 审计标记至今未修复 |

### B-2 · 代码与设计文档的偏差（阶段 13/14 遗留，本轮未修复）

| 编号 | 问题 | 严重度 | 说明 |
|------|------|--------|------|
| **H-P1-1** | `SeatService.GetHomeStatsAsync` 未按日期筛选已占用座位 | P1 | `SeatService.cs:22-23` 缺少 `r.ReservationDate == now.Date` |
| **H-P1-2** | `SeatService.GetAllSeatsAsync` 缺少日期筛选 | P1 | `SeatService.cs:38-39` 同上 |
| **H-P1-3** | `SeatService.GetAllSeatsForAdminAsync` 缺少日期筛选 | P1 | `SeatService.cs:89-90` 同上 |
| **H-P1-4** | `SeatService.GetSeatDetailAsync` 缺少日期筛选 | P1 | `SeatService.cs:60-61` 同上 |
| **H-P1-5** | `AdminService.GetStatisticsAsync` 热门区域未限定当天 | P1 | `AdminService.cs:42-43` 统计的是历史全部预约 |
| **H-P1-6** | `ReservationsController.Create GET` 默认日期使用 UTC | P1 | `ReservationsController.cs:40` 使用 `DateTime.UtcNow.Date.AddDays(1)` |
| **H-P1-7** | `ReservationService.CreateReservationAsync` 日期校验使用 UTC | P1 | `ReservationService.cs:28` 使用 `DateTime.UtcNow.Date` |
| **H-P1-10** | `_Layout.cshtml` 导航未区分已登录/未登录态 | P1 | 用户端导航栏无登录状态感知，docs/05 PU-01 要求已登录显示欢迎语 |
| **H-P1-12** | `AdminService.GetStatisticsAsync` 未按 D08-8.3 一次查询策略 | P1 | 4 次独立查询 |

### B-3 · 文档层面问题

| 编号 | 问题 | 严重度 | 说明 |
|------|------|--------|------|
| **D-1** | `Docs/15-功能完善与体验优化记录.md` 不存在 | P0 | 阶段 15 无开发记录，无法追溯执行过程 |
| **D-2** | 任务板 Sprint 5/6 全部 12 张任务卡仍为"待开发" | P0 | `Docs/项目任务板与迭代记录.md` 无任何状态变更 |
| **D-3** | README.md 未更新 | P1 | 仍为阶段 14 状态描述，未体现阶段 15 内容 |
| **D-4** | 任务板 Sprint 4 分区完成数与 docs/14 不一致（历史遗留） | P2 | Sprint 4 显示"9/9"但 docs/14 Sprint 4 为"6/6" |

### B-4 · 非阻塞历史问题（记录为后续修订项）

| 编号 | 问题 | 说明 |
|------|------|------|
| H-1 | 座位状态标签颜色未按 D05 规范 | D05-1.2 规定浅底深字 pill 样式，实际使用 Bootstrap 默认 badge |
| H-2 | `StudentContext.Logout()` 使用 `Session.Clear()` | 应移除特定键值 |
| H-3 | 项目存在两个独立代码库 | 根目录旧项目与 src 新项目共存 |

---

## C. 必改项

### C-1 · P0-01 唯一索引修复（跨阶段遗留第 3 轮）

**位置**：`src/LibrarySeatReservation.Web/DataAccess/AppDbContext.cs:45-46`

**当前代码**：
```csharp
e.HasIndex(r => new { r.SeatId, r.ReservationDate, r.StartTime, r.EndTime })
    .HasDatabaseName("IX_Reservation_Seat_Time");
```

**应改为**：
```csharp
e.HasIndex(r => new { r.SeatId, r.ReservationDate, r.StartTime, r.EndTime })
    .IsUnique()
    .HasDatabaseName("IX_Reservation_Seat_Time");
```

**同步修复**：
1. 重新生成 EF Core 迁移（`dotnet ef migrations add FixUniqueIndex`）
2. 更新任务板 P0-01 一致性修订跟踪状态

### C-2 · Edit Modal 楼层字段修复（跨阶段遗留第 2 轮）

需同步修复 3 个位置：

1. `src/LibrarySeatReservation.Web/Models/ViewModels/SeatItem.cs` — 添加 `public string? Floor { get; set; }`
2. `src/LibrarySeatReservation.Web/Views/Admin/Seats.cshtml:34-39` — 编辑按钮补充 `data-floor="@seat.Floor"`
3. `src/LibrarySeatReservation.Web/Services/SeatService.cs` — `GetAllSeatsForAdminAsync` 的 Select 中赋值 `Floor = s.Floor`

### C-3 · 阶段 15 最低交付（本轮新发现）

| 最低要求 | 说明 |
|----------|------|
| 创建 `Docs/15-功能完善与体验优化记录.md` | 记录阶段 15 执行过程 |
| 完成 T50-01（Error 页面）| `Error.cshtml` 当前仅 6 行默认模板 |
| 完成 T50-02（404 页面）| 当前不存在任何 404 页面 |
| 完成 T50-03（空状态优化）| 座位列表/预约列表无空状态提示 |
| 更新任务板 Sprint 5 状态 | 将已完成卡片移至"✅ 已完成" |
| 更新 README.md | 体现阶段 15 完成状态 |

---

## D. 建议优化项

| 编号 | 建议 | 优先级 |
|------|------|--------|
| O-1 | 修复阶段 13/14 遗留的 P1-1~P1-5（日期筛选）和 P1-6~P1-7（时区问题） | 中 |
| O-2 | `_Layout.cshtml` 用户端导航增加登录态判断（欢迎语 + 退出） | 中 |
| O-3 | `AdminService.GetStatisticsAsync` 热门区域添加 `r.ReservationDate == today` | 中 |
| O-4 | `site.css` 补充业务样式（错误页、空状态、pill 标签等） | 低 |
| O-5 | `site.js` 迁移内联脚本，统一管理前端逻辑 | 低 |
| O-6 | 座位状态标签改用 D05 定义的浅底深字 pill 样式 | 低 |
| O-7 | `StudentContext.Logout()` 改为 `Session.Remove()` 移除特定键 | 低 |

---

## E. 是否可以进入下一步

**不可以。阶段 15 无任何实质交付，不满足最低完成线。**

### 当前状态

| 条件 | 当前状态 | 必要性 |
|------|----------|--------|
| 开发记录文档存在 | ❌ 不存在 | 必须 |
| Sprint 5 至少 3 张核心任务卡完成 | ❌ 0/8 完成 | 必须 |
| Error 页面（T50-01）| ❌ 未实现 | 必须 |
| 404 页面（T50-02）| ❌ 未实现 | 必须 |
| 空状态优化（T50-03）| ❌ 未实现 | 必须 |
| 任务板 Sprint 5 状态更新 | ❌ 未更新 | 必须 |
| README.md 更新 | ❌ 未更新 | 必须 |
| P0-01 唯一索引修复 | ❌ 未修复（第 3 轮） | 必须 |
| P0-02 Edit Modal 楼层 Bug 修复 | ❌ 未修复（第 2 轮） | 必须 |

### 建议执行路径

```
第一步（必须）：创建 Docs/15-功能完善与体验优化记录.md
第二步（必须）：修复 P0-01（.IsUnique()）+ 生成迁移
第三步（必须）：修复 P0-02（Edit Modal 楼层 Bug）
第四步（必须）：完成 T50-01（Error 页面 + 404 页面）
第五步（必须）：完成 T50-02（空状态优化）
第六步（建议）：完成 T50-03 ~ T50-08 其余 Sprint 5 任务卡
第七步（建议）：修复 P1-1~P1-7 日期筛选和时区问题
第八步：更新任务板 Sprint 5 状态 + 更新 README
第九步：重新提交审计
```

**判断依据**：阶段 15 未产生任何交付物，相当于整个阶段被跳过。必须从头执行，至少完成 Sprint 5 的 3 张核心任务卡（Error 页面、404 页面、空状态优化）并修复跨阶段遗留的 2 个 P0 问题后，方可重新提交审计。

---

*审计人：opencode*
*审计日期：2026-07-13*
